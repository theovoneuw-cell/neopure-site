{
  "name": "neopure-site",
  "version": "1.0.0",
  "private": true,
  "description": "Site vitrine Neopure — build statique Eleventy, contenu éditable via Pages CMS.",
  "scripts": {
    "build": "eleventy",
    "serve": "eleventy --serve"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.0.0"
  }
}
